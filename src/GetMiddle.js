// function Player() {
// }
// Player.prototype.play = function(song) {
//   this.currentlyPlayingSong = song;
//   this.isPlaying = true;
// };

GetMiddle.prototype.getLetter = function(str) {

  var middle = Math.floor(str.length / 2)
    return str.charAt(middle -1)
  
}